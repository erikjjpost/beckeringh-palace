(* Beckeringh Architectuurtaal — generieke syntaxis; semantiek staat in het World Model *)

document        = declaration, { declaration } ;
declaration     = kind, identifier, "{", { property }, "}" ;
kind            = identifier ;
property        = identifier, ":", value ;
value           = scalar | list ;
list            = "[", [ scalar, { ",", scalar } ], "]" ;
scalar          = quoted_string | identifier ;
identifier      = letter, { letter | digit | "_" | "-" | "." } ;
quoted_string   = '"', { ? ieder teken behalve een dubbel aanhalingsteken ? }, '"' ;
letter          = "A"…"Z" | "a"…"z" | "À"…"ÿ" ;
digit           = "0"…"9" ;
