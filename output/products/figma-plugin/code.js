/* Beckeringh Palace Figma adapter. The compiler replaces the marker below. */
const BP_MANIFEST = {"appearances":[{"id":"forge-app-tile-disabled-appearance","naam":"Forge App Tile Disabled","rollen":{"accent":"disabled","body-style":"body","border":"regular","caption-style":"caption","foreground":"disabled","heading-style":"heading","label-style":"label","material":"raised","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"none","spacing":"medium"}},{"id":"forge-app-tile-ember-focus-appearance","naam":"Forge Ember App Tile Focus","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"accent","radius":"medium","shadow":"glow-accent","spacing":"medium"}},{"id":"forge-app-tile-ember-hover-appearance","naam":"Forge Ember App Tile Hover","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"hover","outline":"accent","radius":"medium","shadow":"glow-accent","spacing":"medium"}},{"id":"forge-app-tile-ember-pressed-appearance","naam":"Forge Ember App Tile Pressed","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"accent","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-app-tile-ember-rest-appearance","naam":"Forge Ember App Tile Rest","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-app-tile-focus-appearance","naam":"Forge App Tile Focus","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"interaction","radius":"medium","shadow":"glow","spacing":"medium"}},{"id":"forge-app-tile-hover-appearance","naam":"Forge App Tile Hover","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"hover","outline":"interaction","radius":"medium","shadow":"glow","spacing":"medium"}},{"id":"forge-app-tile-pressed-appearance","naam":"Forge App Tile Pressed","rollen":{"accent":"interaction-pressed","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"interaction-pressed","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-app-tile-rest-appearance","naam":"Forge App Tile Rest","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-button-disabled-appearance","naam":"Forge Button Disabled","rollen":{"accent":"disabled","body-style":"body","border":"regular","caption-style":"caption","foreground":"disabled","heading-style":"heading","label-style":"label","material":"raised","motion":"normal","offset":"rest","outline":"outline","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-ember-focus-appearance","naam":"Forge Ember Button Focus","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"accent","motion":"normal","offset":"rest","outline":"accent","radius":"pill","shadow":"glow-accent","spacing":"small"}},{"id":"forge-button-ember-hover-appearance","naam":"Forge Ember Button Hover","rollen":{"accent":"accent-hover","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"accent-hover","motion":"normal","offset":"rest","outline":"accent-hover","radius":"pill","shadow":"glow-accent","spacing":"small"}},{"id":"forge-button-ember-pressed-appearance","naam":"Forge Ember Button Pressed","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"accent","motion":"normal","offset":"rest","outline":"accent","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-ember-rest-appearance","naam":"Forge Ember Button Rest","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"accent","motion":"normal","offset":"rest","outline":"accent","radius":"pill","shadow":"glow-accent","spacing":"small"}},{"id":"forge-button-ghost-hover-appearance","naam":"Forge Ghost Button Hover","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"interaction-soft","motion":"normal","offset":"rest","outline":"transparent","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-ghost-rest-appearance","naam":"Forge Ghost Button Rest","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"muted","heading-style":"heading","label-style":"label","material":"transparent","motion":"normal","offset":"rest","outline":"transparent","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-primary-focus-appearance","naam":"Forge Primary Button Focus","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"interaction","motion":"normal","offset":"rest","outline":"interaction","radius":"pill","shadow":"focus","spacing":"small"}},{"id":"forge-button-primary-hover-appearance","naam":"Forge Primary Button Hover","rollen":{"accent":"interaction-hover","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"interaction-hover","motion":"normal","offset":"rest","outline":"interaction-hover","radius":"pill","shadow":"glow","spacing":"small"}},{"id":"forge-button-primary-pressed-appearance","naam":"Forge Primary Button Pressed","rollen":{"accent":"interaction-pressed","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"interaction-pressed","motion":"normal","offset":"rest","outline":"interaction-pressed","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-primary-rest-appearance","naam":"Forge Primary Button Rest","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"canvas","heading-style":"heading","label-style":"label","material":"interaction","motion":"normal","offset":"rest","outline":"interaction","radius":"pill","shadow":"low","spacing":"small"}},{"id":"forge-button-secondary-focus-appearance","naam":"Forge Secondary Button Focus","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"interaction","heading-style":"heading","label-style":"label","material":"transparent","motion":"normal","offset":"rest","outline":"interaction","radius":"pill","shadow":"focus","spacing":"small"}},{"id":"forge-button-secondary-hover-appearance","naam":"Forge Secondary Button Hover","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"interaction","heading-style":"heading","label-style":"label","material":"transparent","motion":"normal","offset":"rest","outline":"interaction","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-secondary-pressed-appearance","naam":"Forge Secondary Button Pressed","rollen":{"accent":"interaction-pressed","body-style":"body","border":"regular","caption-style":"caption","foreground":"interaction-pressed","heading-style":"heading","label-style":"label","material":"transparent","motion":"normal","offset":"rest","outline":"interaction-pressed","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-button-secondary-rest-appearance","naam":"Forge Secondary Button Rest","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"transparent","motion":"normal","offset":"rest","outline":"outline","radius":"pill","shadow":"none","spacing":"small"}},{"id":"forge-input-disabled-appearance","naam":"Forge Input Disabled","rollen":{"accent":"disabled","body-style":"body","border":"regular","caption-style":"caption","foreground":"disabled","heading-style":"heading","label-style":"label","material":"field","motion":"normal","offset":"rest","outline":"outline","radius":"control","shadow":"none","spacing":"small"}},{"id":"forge-input-error-appearance","naam":"Forge Input Error","rollen":{"accent":"error","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"field","motion":"normal","offset":"rest","outline":"error","radius":"control","shadow":"none","spacing":"small"}},{"id":"forge-input-focus-appearance","naam":"Forge Input Focus","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"field","motion":"normal","offset":"rest","outline":"interaction","radius":"control","shadow":"focus","spacing":"small"}},{"id":"forge-input-rest-appearance","naam":"Forge Input Rest","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"field","motion":"normal","offset":"rest","outline":"outline","radius":"control","shadow":"none","spacing":"small"}},{"id":"forge-panel-appearance","naam":"Forge Panel Appearance","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"raised","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"medium","spacing":"small"}},{"id":"forge-panel-card-disabled-appearance","naam":"Forge Panel Card Disabled Appearance","rollen":{"accent":"disabled","body-style":"body","border":"regular","caption-style":"caption","foreground":"disabled","heading-style":"heading","label-style":"label","material":"raised","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"none","spacing":"medium"}},{"id":"forge-panel-card-focus-appearance","naam":"Forge Panel Card Focus Appearance","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"interaction","radius":"medium","shadow":"glow","spacing":"medium"}},{"id":"forge-panel-card-hover-appearance","naam":"Forge Panel Card Hover Appearance","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"hover","outline":"interaction","radius":"medium","shadow":"glow","spacing":"medium"}},{"id":"forge-panel-card-pressed-appearance","naam":"Forge Panel Card Pressed Appearance","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"interaction-pressed","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-panel-card-rest-appearance","naam":"Forge Panel Card Rest Appearance","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"normal","offset":"rest","outline":"outline","radius":"medium","shadow":"low","spacing":"medium"}},{"id":"forge-panel-hero-appearance","naam":"Forge Panel Hero Appearance","rollen":{"accent":"accent","body-style":"body","border":"strong","caption-style":"caption","foreground":"foreground","heading-style":"display","label-style":"label","material":"raised","motion":"slow","offset":"rest","outline":"outline","radius":"large","shadow":"high","spacing":"xl"}},{"id":"forge-stat-card-ember-appearance","naam":"Forge Stat Card Ember","rollen":{"accent":"accent","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"fast","offset":"rest","outline":"outline","radius":"medium","shadow":"none","spacing":"medium"}},{"id":"forge-stat-card-health-appearance","naam":"Forge Stat Card Health","rollen":{"accent":"success","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"fast","offset":"rest","outline":"outline","radius":"medium","shadow":"none","spacing":"medium"}},{"id":"forge-stat-card-value-appearance","naam":"Forge Stat Card Value","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"surface","motion":"fast","offset":"rest","outline":"outline","radius":"medium","shadow":"none","spacing":"medium"}},{"id":"forge-status-failed-appearance","naam":"Forge Failed Status","rollen":{"accent":"error","body-style":"body","border":"regular","caption-style":"caption","foreground":"error-foreground","heading-style":"heading","label-style":"label","material":"error-surface","motion":"fast","offset":"rest","outline":"error","radius":"pill","shadow":"none","spacing":"xs"}},{"id":"forge-status-info-appearance","naam":"Forge Info Status","rollen":{"accent":"info","body-style":"body","border":"regular","caption-style":"caption","foreground":"info-foreground","heading-style":"heading","label-style":"label","material":"info-surface","motion":"fast","offset":"rest","outline":"info","radius":"pill","shadow":"none","spacing":"xs"}},{"id":"forge-status-pending-appearance","naam":"Forge Pending Status","rollen":{"accent":"warning","body-style":"body","border":"regular","caption-style":"caption","foreground":"warning-foreground","heading-style":"heading","label-style":"label","material":"warning-surface","motion":"fast","offset":"rest","outline":"warning","radius":"pill","shadow":"none","spacing":"xs"}},{"id":"forge-status-running-appearance","naam":"Forge Running Status","rollen":{"accent":"success","body-style":"body","border":"regular","caption-style":"caption","foreground":"success-foreground","heading-style":"heading","label-style":"label","material":"success-surface","motion":"fast","offset":"rest","outline":"success","radius":"pill","shadow":"none","spacing":"xs"}},{"id":"forge-terminal-appearance","naam":"Forge Terminal Appearance","rollen":{"accent":"interaction","body-style":"body","border":"regular","caption-style":"caption","foreground":"foreground","heading-style":"heading","label-style":"label","material":"field","motion":"slow","offset":"rest","outline":"outline","radius":"medium","shadow":"high","spacing":"none"}}],"assets":[{"id":"emberforge-vector-node","label":null,"lijn":"currentColor","lijndikte":2.0,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Vector Node","paden":["M32 6 L38 24 L56 32 L38 40 L32 58 L26 40 L8 32 L26 24 Z","M32 22 L42 32 L32 42 L22 32 Z"],"rol":"ornament","toegankelijkheid":"decoratief","viewbox":[0.0,0.0,64.0,64.0],"vulling":"none"},{"id":"emberforge-light-disc","label":null,"lijn":"none","lijndikte":null,"lijneinde":null,"lijnverbinding":null,"naam":"EmberForge Lichtschijf","paden":["M500 20 A480 480 0 1 0 500 980 A480 480 0 1 0 500 20 Z"],"rol":"ornament","toegankelijkheid":"decoratief","viewbox":[0.0,0.0,1000.0,1000.0],"vulling":"currentColor"},{"id":"emberforge-palace","label":null,"lijn":"currentColor","lijndikte":4.0,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Palace lijnsilhouet","paden":["M40 310 H920","M180 250 L480 70 L780 250 M140 250 H820 M180 250 V310 M780 250 V310","M40 310 V235 L150 165 L260 235 V310 M700 235 L810 165 L920 235 V310","M300 245 L480 130 L660 245 M340 245 L480 160 L620 245","M430 310 V230 H530 V310","M150 165 V120 M480 70 V35 M810 165 V120","M320 310 V265 M640 310 V265"],"rol":"illustratie","toegankelijkheid":"decoratief","viewbox":[0.0,0.0,960.0,360.0],"vulling":"none"},{"id":"emberforge-beaver","label":null,"lijn":"currentColor","lijndikte":5.0,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge bever","paden":["M79 205 C67 187 63 166 68 146 C74 123 90 104 112 94 C127 87 144 84 160 84 C176 84 193 87 208 94 C230 104 246 123 252 146 C257 166 253 187 241 205 C226 226 196 238 160 238 C124 238 94 226 79 205 Z","M103 104 C91 105 82 97 82 86 C82 75 91 66 102 66 C115 66 123 77 121 90 M217 104 C229 105 238 97 238 86 C238 75 229 66 218 66 C205 66 197 77 199 90","M119 139 C124 134 132 134 137 139 M183 139 C188 134 196 134 201 139","M153 158 C153 151 159 146 166 146 C173 146 179 151 179 158 C179 166 171 172 166 175 C161 172 153 166 153 158 Z","M166 175 C154 183 143 187 131 185 M166 175 C178 183 189 187 201 185","M145 186 V211 H160 V188 M160 188 V211 H175 V186 M145 211 H175","M109 173 C99 178 91 187 88 198 M223 173 C233 178 241 187 244 198","M83 209 C55 209 31 224 21 247 C11 270 24 294 48 299 C72 304 98 289 116 250","M32 236 L88 286 M22 258 L67 298 M55 219 L108 267","M109 239 H211 L203 254 H187 L181 265 H139 L133 254 H117 Z","M127 265 H193 M142 265 L135 286 H185 L178 265","M290 305 V195","M262 178 H318 V203 H262 Z"],"rol":"illustratie","toegankelijkheid":"decoratief","viewbox":[0.0,0.0,320.0,320.0],"vulling":"none"},{"id":"emberforge-nordic-weave","label":null,"lijn":"currentColor","lijndikte":3.0,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Noorse vlecht","paden":["M20 80 L80 20 L160 100 L240 20 L320 100 L400 20 L480 100 L560 20 L640 100 L700 40","M20 80 L80 140 L160 60 L240 140 L320 60 L400 140 L480 60 L560 140 L640 60 L700 120"],"rol":"ornament","toegankelijkheid":"decoratief","viewbox":[0.0,0.0,720.0,160.0],"vulling":"none"},{"id":"emberforge-icon-dashboard","label":"Dashboard","lijn":"currentColor","lijndikte":1.5,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Dashboard Icon","paden":["M4 4 L10 4 L10 10 L4 10 Z","M14 4 L20 4 L20 10 L14 10 Z","M4 14 L10 14 L10 20 L4 20 Z","M14 14 L20 14 L20 20 L14 20 Z"],"rol":"icoon","toegankelijkheid":"informatief","viewbox":[0.0,0.0,24.0,24.0],"vulling":"none"},{"id":"emberforge-icon-identity","label":"Identity","lijn":"currentColor","lijndikte":1.5,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Identity Icon","paden":["M12 3 C15 3 17 5 17 8 C17 11 15 13 12 13 C9 13 7 11 7 8 C7 5 9 3 12 3 Z","M4 21 C4 17 7 15 12 15 C17 15 20 17 20 21"],"rol":"icoon","toegankelijkheid":"informatief","viewbox":[0.0,0.0,24.0,24.0],"vulling":"none"},{"id":"emberforge-icon-terminal","label":"Terminal","lijn":"currentColor","lijndikte":1.5,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Terminal Icon","paden":["M4 5 L20 5 L20 19 L4 19 Z","M7 9 L10 12 L7 15","M12 15 L16 15"],"rol":"icoon","toegankelijkheid":"informatief","viewbox":[0.0,0.0,24.0,24.0],"vulling":"none"},{"id":"emberforge-icon-assets","label":"Assets","lijn":"currentColor","lijndikte":1.5,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge Assets Icon","paden":["M12 3 L20 7 L20 17 L12 21 L4 17 L4 7 Z","M4 7 L12 11 L20 7","M12 11 L12 21"],"rol":"icoon","toegankelijkheid":"informatief","viewbox":[0.0,0.0,24.0,24.0],"vulling":"none"},{"id":"emberforge-merkteken","label":"EmberForge","lijn":"currentColor","lijndikte":2.5,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge merkteken","paden":["M 40.06 10.24 Q 48 6 55.94 10.24 L 70.06 17.76 Q 78 22 80.85 30.54 L 85.15 43.46 Q 88 52 83.76 59.94 L 76.24 74.06 Q 72 82 63.69 85.46 L 56.31 88.54 Q 48 92 39.69 88.54 L 32.31 85.46 Q 24 82 19.76 74.06 L 12.24 59.94 Q 8 52 10.85 43.46 L 15.15 30.54 Q 18 22 25.94 17.76 L 40.06 10.24 Z","M48 18 C59 29 63 38 59 48 C57 54 52 58 48 62 C43 58 38 53 36 47 C33 39 38 31 44 25 C44 34 47 39 51 42 C54 34 53 26 48 18 Z","M 27.8 64.4 Q 26 62 29 62 L 67 62 Q 70 62 68.2 64.4 L 65.8 67.6 Q 64 70 61 70 L 58 70 Q 55 70 55 73 L 55 77 Q 55 80 52 80 L 44 80 Q 41 80 41 77 L 41 73 Q 41 70 38 70 L 35 70 Q 32 70 30.2 67.6 L 27.8 64.4 Z"],"rol":"logo","toegankelijkheid":"informatief","viewbox":[0.0,0.0,96.0,96.0],"vulling":"none"},{"id":"emberforge-woordmerk","label":"EmberForge","lijn":"currentColor","lijndikte":3.0,"lijneinde":"round","lijnverbinding":"round","naam":"EmberForge woordmerk","paden":["M12 16 V64 M12 16 H50 M12 40 H44 M12 64 H50","M66 64 V16 L86 42 L106 16 V64","M124 16 V64 M124 16 H148 C158 16 162 22 162 28 C162 34 158 40 148 40 H124 M148 40 C158 40 164 46 164 52 C164 59 159 64 148 64 H124","M182 16 V64 M182 16 H220 M182 40 H214 M182 64 H220","M238 64 V16 H258 C269 16 274 22 274 28 C274 36 269 40 258 40 H238 M256 40 L276 64","M296 64 V16 H334 M296 40 H328","M368 16 H384 C395 16 400 24 400 40 C400 56 395 64 384 64 H368 C357 64 352 56 352 40 C352 24 357 16 368 16 Z","M418 64 V16 H438 C449 16 454 22 454 28 C454 36 449 40 438 40 H418 M436 40 L456 64","M524 26 C519 19 512 16 500 16 H492 C481 16 476 24 476 40 C476 56 481 64 492 64 H510 C519 64 524 58 524 48 V40 H506","M542 16 V64 M542 16 H580 M542 40 H574 M542 64 H580"],"rol":"logo","toegankelijkheid":"informatief","viewbox":[0.0,0.0,592.0,80.0],"vulling":"none"}],"components":[{"anatomie":["titel","tekst"],"appearance":"forge-panel-appearance","id":"forge-panel","naam":"Forge Panel","rol":"paneel","toegankelijkheid":"forge-panel-accessibility"},{"anatomie":["label"],"appearance":"forge-button-primary-rest-appearance","id":"forge-button","naam":"Forge Button","rol":"actie","toegankelijkheid":"forge-button-accessibility"},{"anatomie":["label","waarde","melding"],"appearance":"forge-input-rest-appearance","id":"forge-input","naam":"Forge Input","rol":"invoer","toegankelijkheid":"forge-input-accessibility"},{"anatomie":["label","waarde"],"appearance":"forge-status-info-appearance","id":"forge-status","naam":"Forge Status","rol":"status","toegankelijkheid":"forge-status-accessibility"},{"anatomie":["label","beschrijving","status"],"appearance":"forge-app-tile-rest-appearance","id":"forge-app-tile","naam":"Forge App Tile","rol":"app-tegel","toegankelijkheid":"forge-app-tile-accessibility"},{"anatomie":["label","waarde","beschrijving"],"appearance":"forge-stat-card-value-appearance","id":"forge-stat-card","naam":"Forge Stat Card","rol":"statistiek","toegankelijkheid":"forge-stat-card-accessibility"},{"anatomie":["label","venstertitel","vensterknoppen","tabs","actieve-tab","markering","gebruiker","host","sleutels","waarden","pad","prompt","cursor"],"appearance":"forge-terminal-appearance","id":"forge-terminal","naam":"Forge Terminal","rol":"terminal","toegankelijkheid":"forge-terminal-accessibility"}],"compositions":[{"id":"forge-dashboard","instances":[{"appearance":"forge-panel-appearance","component":"forge-panel","id":"forge-dashboard-left-panel","variant":null},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"forge-dashboard-center-panel","variant":"forge-panel-compact"},{"appearance":"forge-panel-appearance","component":"forge-panel","id":"forge-dashboard-right-panel","variant":null}],"naam":"Forge Dashboard","rol":null},{"id":"emberforge-observatory","instances":[{"appearance":"forge-stat-card-value-appearance","component":"forge-stat-card","id":"homelab-stat-nodes","variant":"forge-stat-card-value"},{"appearance":"forge-stat-card-health-appearance","component":"forge-stat-card","id":"homelab-stat-health","variant":"forge-stat-card-health"},{"appearance":"forge-stat-card-value-appearance","component":"forge-stat-card","id":"homelab-stat-cpu","variant":"forge-stat-card-progress"},{"appearance":"forge-stat-card-ember-appearance","component":"forge-stat-card","id":"homelab-stat-memory","variant":"forge-stat-card-progress-ember"},{"appearance":"forge-status-running-appearance","component":"forge-status","id":"homelab-status-running","variant":"forge-status-running"},{"appearance":"forge-status-pending-appearance","component":"forge-status","id":"homelab-status-pending","variant":"forge-status-pending"},{"appearance":"forge-status-failed-appearance","component":"forge-status","id":"homelab-status-failed","variant":"forge-status-failed"},{"appearance":"forge-status-info-appearance","component":"forge-status","id":"homelab-status-healthy","variant":"forge-status-info"},{"appearance":"forge-app-tile-rest-appearance","component":"forge-app-tile","id":"homelab-app-isms","variant":"forge-app-tile-default"},{"appearance":"forge-app-tile-ember-rest-appearance","component":"forge-app-tile","id":"homelab-app-cv","variant":"forge-app-tile-ember"}],"naam":"The Observatory","rol":null},{"id":"emberforge-keycloak-login","instances":[{"appearance":"forge-input-rest-appearance","component":"forge-input","id":"keycloak-login-identity","variant":"forge-input-default"},{"appearance":"forge-input-rest-appearance","component":"forge-input","id":"keycloak-login-password","variant":"forge-input-default"},{"appearance":"forge-button-primary-rest-appearance","component":"forge-button","id":"keycloak-login-submit","variant":"forge-button-primary"}],"naam":"EmberForge Sign In","rol":"login-formulier"},{"id":"emberforge-terminal","instances":[{"appearance":"forge-terminal-appearance","component":"forge-terminal","id":"emberforge-terminal-session","variant":"forge-terminal-neofetch"}],"naam":"EmberForge Terminal","rol":"terminal-sessie"},{"id":"forge-design-system-reference-composition","instances":[{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"forge-design-system-reference-content","variant":"forge-panel-compact"}],"naam":"EmberForge Design System Referentie","rol":null},{"id":"emberforge-svg-asset-catalog-composition","instances":[{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"emberforge-svg-asset-catalog-content","variant":"forge-panel-compact"}],"naam":"EmberForge SVG Asset Catalog","rol":null},{"id":"beckeringh-palace-homepage-composition","instances":[{"appearance":"forge-panel-hero-appearance","component":"forge-panel","id":"homepage-intro","variant":"forge-panel-hero"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-world","variant":"forge-panel-route"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-design-system","variant":"forge-panel-route"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-svg-assets","variant":"forge-panel-route"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-project-status","variant":"forge-panel-route"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-keycloak","variant":"forge-panel-route"},{"appearance":"forge-panel-card-rest-appearance","component":"forge-panel","id":"homepage-terminal","variant":"forge-panel-route"}],"naam":"Beckeringh Palace","rol":null}],"figma_master":{"doel":"Selecteert expliciet dezelfde opgeloste ontwerpsemantiek voor een reproduceerbare Figma masterbeschrijving zonder live Figma synchronisatie.","id":"emberforge-figma-master","naam":"EmberForge Figma Master","wereld":"beckeringh-palace"},"layouts":[{"columns":3,"compact_columns":null,"direction":null,"id":"forge-dashboard-ultrawide","naam":"Forge Dashboard Ultrawide","regions":[{"column":1,"column_span":1,"compact_order":null,"id":"forge-dashboard-left","instance":"forge-dashboard-left-panel","layer":null,"row":1,"row_span":1},{"column":2,"column_span":1,"compact_order":null,"id":"forge-dashboard-center","instance":"forge-dashboard-center-panel","layer":null,"row":1,"row_span":1},{"column":3,"column_span":1,"compact_order":null,"id":"forge-dashboard-right","instance":"forge-dashboard-right-panel","layer":null,"row":1,"row_span":1}],"responsive_breakpoint":null,"rows":1,"type":"grid","wrap":null},{"columns":4,"compact_columns":2,"direction":null,"id":"emberforge-observatory-responsive","naam":"The Observatory Responsive","regions":[{"column":1,"column_span":1,"compact_order":1,"id":"homelab-stat-nodes-region","instance":"homelab-stat-nodes","layer":null,"row":1,"row_span":1},{"column":2,"column_span":1,"compact_order":2,"id":"homelab-stat-health-region","instance":"homelab-stat-health","layer":null,"row":1,"row_span":1},{"column":3,"column_span":1,"compact_order":3,"id":"homelab-stat-cpu-region","instance":"homelab-stat-cpu","layer":null,"row":1,"row_span":1},{"column":4,"column_span":1,"compact_order":4,"id":"homelab-stat-memory-region","instance":"homelab-stat-memory","layer":null,"row":1,"row_span":1},{"column":1,"column_span":1,"compact_order":5,"id":"homelab-status-running-region","instance":"homelab-status-running","layer":null,"row":2,"row_span":1},{"column":2,"column_span":1,"compact_order":6,"id":"homelab-status-pending-region","instance":"homelab-status-pending","layer":null,"row":2,"row_span":1},{"column":3,"column_span":1,"compact_order":7,"id":"homelab-status-failed-region","instance":"homelab-status-failed","layer":null,"row":2,"row_span":1},{"column":4,"column_span":1,"compact_order":8,"id":"homelab-status-healthy-region","instance":"homelab-status-healthy","layer":null,"row":2,"row_span":1},{"column":1,"column_span":2,"compact_order":9,"id":"homelab-app-isms-region","instance":"homelab-app-isms","layer":null,"row":3,"row_span":1},{"column":3,"column_span":2,"compact_order":10,"id":"homelab-app-cv-region","instance":"homelab-app-cv","layer":null,"row":3,"row_span":1}],"responsive_breakpoint":960,"rows":3,"type":"grid","wrap":null},{"columns":1,"compact_columns":1,"direction":null,"id":"emberforge-keycloak-login-responsive","naam":"EmberForge Keycloak Login Responsive","regions":[{"column":1,"column_span":1,"compact_order":1,"id":"keycloak-login-identity-region","instance":"keycloak-login-identity","layer":null,"row":1,"row_span":1},{"column":1,"column_span":1,"compact_order":2,"id":"keycloak-login-password-region","instance":"keycloak-login-password","layer":null,"row":2,"row_span":1},{"column":1,"column_span":1,"compact_order":3,"id":"keycloak-login-submit-region","instance":"keycloak-login-submit","layer":null,"row":3,"row_span":1}],"responsive_breakpoint":640,"rows":3,"type":"grid","wrap":null},{"columns":null,"compact_columns":null,"direction":"vertical","id":"emberforge-terminal-stack","naam":"EmberForge Terminal Stack","regions":[{"column":null,"column_span":null,"compact_order":null,"id":"emberforge-terminal-region","instance":"emberforge-terminal-session","layer":null,"row":null,"row_span":null}],"responsive_breakpoint":null,"rows":null,"type":"stack","wrap":null},{"columns":null,"compact_columns":null,"direction":"vertical","id":"forge-design-system-reference-stack","naam":"EmberForge Referentie Stack","regions":[{"column":null,"column_span":null,"compact_order":null,"id":"forge-design-system-reference-region","instance":"forge-design-system-reference-content","layer":null,"row":null,"row_span":null}],"responsive_breakpoint":null,"rows":null,"type":"stack","wrap":null},{"columns":null,"compact_columns":null,"direction":"vertical","id":"emberforge-svg-asset-catalog-stack","naam":"EmberForge SVG Asset Catalog Stack","regions":[{"column":null,"column_span":null,"compact_order":null,"id":"emberforge-svg-asset-catalog-region","instance":"emberforge-svg-asset-catalog-content","layer":null,"row":null,"row_span":null}],"responsive_breakpoint":null,"rows":null,"type":"stack","wrap":null},{"columns":6,"compact_columns":1,"direction":null,"id":"beckeringh-palace-homepage-grid","naam":"Beckeringh Palace Homepage Grid","regions":[{"column":1,"column_span":6,"compact_order":1,"id":"homepage-intro-region","instance":"homepage-intro","layer":null,"row":1,"row_span":1},{"column":1,"column_span":2,"compact_order":2,"id":"homepage-world-region","instance":"homepage-world","layer":null,"row":2,"row_span":1},{"column":3,"column_span":2,"compact_order":3,"id":"homepage-design-system-region","instance":"homepage-design-system","layer":null,"row":2,"row_span":1},{"column":5,"column_span":2,"compact_order":4,"id":"homepage-svg-assets-region","instance":"homepage-svg-assets","layer":null,"row":2,"row_span":1},{"column":1,"column_span":2,"compact_order":5,"id":"homepage-project-status-region","instance":"homepage-project-status","layer":null,"row":3,"row_span":1},{"column":3,"column_span":2,"compact_order":6,"id":"homepage-keycloak-region","instance":"homepage-keycloak","layer":null,"row":3,"row_span":1},{"column":5,"column_span":2,"compact_order":7,"id":"homepage-terminal-region","instance":"homepage-terminal","layer":null,"row":3,"row_span":1}],"responsive_breakpoint":960,"rows":3,"type":"grid","wrap":null}],"product":{"id":"emberforge-figma-master-manifest","snapshot":"sha256:d446ac06fa50470d0233a440b93f668a0638066d79d964040179eec2d37fbcde"},"schema_version":2,"theme":{"art_direction":{"canvas":{"color_id":"ink-900","kleur":"#0F1724","rol":"canvas"},"density":"spacious","glow":"controlled","id":"emberforge-art-direction","imagery":"isometric-line-art","interaction":{"color_id":"sky-400","kleur":"#7DD3FC","rol":"primary"},"ornament":"technical-linework","warm_accent":{"color_id":"ember-500","kleur":"#C9895B","limiet":2,"rol":"accent"}},"border":{"id":"forge-borders","rollen":{"hairline":"1px","regular":"1px","strong":"1px","style":"solid"}},"color_primitives":{"bone-300":"#B8C5D6","bone-50":"#E6EDF5","ember-300":"#E2A982","ember-500":"#C9895B","ink-500":"#3E5573","ink-600":"#2F4259","ink-700":"#243447","ink-800":"#1F2937","ink-900":"#0F1724","ink-950":"#0A111C","sky-300":"#A5DEFB","sky-400":"#7DD3FC","sky-500":"#38BDF8","sky-soft":"#7DD3FC0F","status-error":"#F87171","status-error-foreground":"#FCA5A5","status-error-surface":"#F871711A","status-info-surface":"#7DD3FC1A","status-success":"#4ADE80","status-success-foreground":"#86EFAC","status-success-surface":"#4ADE801F","status-warning":"#E0B341","status-warning-foreground":"#F3D783","status-warning-surface":"#E0B3411A","transparent-clear":"#00000000"},"id":"forge","materiaal":{"id":"forge-materials","rollen":{"accent":{"color_id":"ember-500","value":"#C9895B"},"accent-hover":{"color_id":"ember-300","value":"#E2A982"},"canvas":{"color_id":"ink-900","value":"#0F1724"},"disabled":{"color_id":"ink-500","value":"#3E5573"},"error":{"color_id":"status-error","value":"#F87171"},"error-foreground":{"color_id":"status-error-foreground","value":"#FCA5A5"},"error-surface":{"color_id":"status-error-surface","value":"#F871711A"},"field":{"color_id":"ink-950","value":"#0A111C"},"foreground":{"color_id":"bone-50","value":"#E6EDF5"},"info":{"color_id":"sky-400","value":"#7DD3FC"},"info-foreground":{"color_id":"sky-300","value":"#A5DEFB"},"info-surface":{"color_id":"status-info-surface","value":"#7DD3FC1A"},"interaction":{"color_id":"sky-400","value":"#7DD3FC"},"interaction-hover":{"color_id":"sky-300","value":"#A5DEFB"},"interaction-pressed":{"color_id":"sky-500","value":"#38BDF8"},"interaction-soft":{"color_id":"sky-soft","value":"#7DD3FC0F"},"muted":{"color_id":"bone-300","value":"#B8C5D6"},"outline":{"color_id":"ink-600","value":"#2F4259"},"raised":{"color_id":"ink-700","value":"#243447"},"success":{"color_id":"status-success","value":"#4ADE80"},"success-foreground":{"color_id":"status-success-foreground","value":"#86EFAC"},"success-surface":{"color_id":"status-success-surface","value":"#4ADE801F"},"surface":{"color_id":"ink-800","value":"#1F2937"},"transparent":{"color_id":"transparent-clear","value":"#00000000"},"warning":{"color_id":"status-warning","value":"#E0B341"},"warning-foreground":{"color_id":"status-warning-foreground","value":"#F3D783"},"warning-surface":{"color_id":"status-warning-surface","value":"#E0B3411A"}}},"motion":{"id":"forge-motion","rollen":{"easing":"cubic-bezier(0.2, 0.7, 0.2, 1)","fast":"120ms","hover_offset":"-1px","normal":"220ms","rest_offset":"0px","slow":"420ms"}},"palet":{"id":"ember-forge","rollen":{"accent":{"color_id":"ember-500","value":"#C9895B"},"background":{"color_id":"ink-900","value":"#0F1724"},"error":{"color_id":"status-error","value":"#F87171"},"foreground":{"color_id":"bone-50","value":"#E6EDF5"},"primary":{"color_id":"sky-400","value":"#7DD3FC"},"success":{"color_id":"status-success","value":"#4ADE80"},"surface":{"color_id":"ink-800","value":"#1F2937"},"warning":{"color_id":"status-warning","value":"#E0B341"}}},"radius":{"id":"forge-radius","rollen":{"control":"8px","large":"16px","medium":"12px","pill":"999px","small":"4px"}},"shadow":{"id":"forge-shadows","rollen":{"focus":"0 0 0 3px rgba(125,211,252,0.15)","glow":"0 0 0 1px rgba(125,211,252,0.18), 0 6px 24px rgba(125,211,252,0.10)","glow_accent":"0 0 0 1px rgba(201,137,91,0.30), 0 6px 22px rgba(201,137,91,0.18)","high":"0 18px 44px rgba(0,0,0,0.45)","low":"0 1px 2px rgba(0,0,0,0.25)","medium":"0 6px 18px rgba(0,0,0,0.35)","none":"none"}},"spacing":{"id":"forge-spacing","rollen":{"large":"32px","medium":"16px","none":"0","small":"8px","xl":"64px","xs":"4px"}},"typeschaal":{"id":"forge-type-scale","rollen":{"body":{"font_family":["Inter","IBM Plex Sans","system-ui","-apple-system","sans-serif"],"font_role":"body","font_size":"16px","font_weight":"400","letter_spacing":"normal","line_height":"1.55"},"caption":{"font_family":["Inter","IBM Plex Sans","system-ui","-apple-system","sans-serif"],"font_role":"body","font_size":"12px","font_weight":"400","letter_spacing":"normal","line_height":"normal"},"display":{"font_family":["Orbitron","Iceland","Bank Gothic","system-ui","sans-serif"],"font_role":"heading","font_size":"80px","font_weight":"500","letter_spacing":"0.08em","line_height":"1"},"heading":{"font_family":["Orbitron","Iceland","Bank Gothic","system-ui","sans-serif"],"font_role":"heading","font_size":"32px","font_weight":"700","letter_spacing":"normal","line_height":"1.3"},"label":{"font_family":["Inter","IBM Plex Sans","system-ui","-apple-system","sans-serif"],"font_role":"body","font_size":"12px","font_weight":"500","letter_spacing":"0.18em","line_height":"normal"},"title":{"font_family":["Orbitron","Iceland","Bank Gothic","system-ui","sans-serif"],"font_role":"heading","font_size":"56px","font_weight":"600","letter_spacing":"-0.01em","line_height":"1.15"}}},"typografie":{"body":["Inter","IBM Plex Sans","system-ui","-apple-system","sans-serif"],"heading":["Orbitron","Iceland","Bank Gothic","system-ui","sans-serif"],"id":"forge-interface","levering":"local-only","mono":["JetBrains Mono","Fira Code","SF Mono","Menlo","monospace"]},"wereld":"beckeringh-palace"},"variants":[{"component":"forge-panel","id":"forge-panel-compact","naam":"Forge Panel Compact","states":{"disabled":"forge-panel-card-disabled-appearance","focus":"forge-panel-card-focus-appearance","hover":"forge-panel-card-hover-appearance","pressed":"forge-panel-card-pressed-appearance","rest":"forge-panel-card-rest-appearance"}},{"component":"forge-panel","id":"forge-panel-hero","naam":"Forge Panel Hero","states":{"rest":"forge-panel-hero-appearance"}},{"component":"forge-panel","id":"forge-panel-route","naam":"Forge Panel Route","states":{"disabled":"forge-panel-card-disabled-appearance","focus":"forge-panel-card-focus-appearance","hover":"forge-panel-card-hover-appearance","pressed":"forge-panel-card-pressed-appearance","rest":"forge-panel-card-rest-appearance"}},{"component":"forge-button","id":"forge-button-primary","naam":"Primary","states":{"disabled":"forge-button-disabled-appearance","focus":"forge-button-primary-focus-appearance","hover":"forge-button-primary-hover-appearance","pressed":"forge-button-primary-pressed-appearance","rest":"forge-button-primary-rest-appearance"}},{"component":"forge-button","id":"forge-button-secondary","naam":"Secondary","states":{"disabled":"forge-button-disabled-appearance","focus":"forge-button-secondary-focus-appearance","hover":"forge-button-secondary-hover-appearance","pressed":"forge-button-secondary-pressed-appearance","rest":"forge-button-secondary-rest-appearance"}},{"component":"forge-button","id":"forge-button-ghost","naam":"Ghost","states":{"disabled":"forge-button-disabled-appearance","focus":"forge-button-secondary-focus-appearance","hover":"forge-button-ghost-hover-appearance","pressed":"forge-button-secondary-pressed-appearance","rest":"forge-button-ghost-rest-appearance"}},{"component":"forge-button","id":"forge-button-ember","naam":"Ember","states":{"disabled":"forge-button-disabled-appearance","focus":"forge-button-ember-focus-appearance","hover":"forge-button-ember-hover-appearance","pressed":"forge-button-ember-pressed-appearance","rest":"forge-button-ember-rest-appearance"}},{"component":"forge-input","id":"forge-input-default","naam":"Default Input","states":{"disabled":"forge-input-disabled-appearance","focus":"forge-input-focus-appearance","hover":"forge-input-rest-appearance","pressed":"forge-input-focus-appearance","rest":"forge-input-rest-appearance"}},{"component":"forge-input","id":"forge-input-error","naam":"Error Input","states":{"rest":"forge-input-error-appearance"}},{"component":"forge-status","id":"forge-status-running","naam":"Actief","states":{"rest":"forge-status-running-appearance"}},{"component":"forge-status","id":"forge-status-pending","naam":"In wachtrij","states":{"rest":"forge-status-pending-appearance"}},{"component":"forge-status","id":"forge-status-failed","naam":"Mislukt","states":{"rest":"forge-status-failed-appearance"}},{"component":"forge-status","id":"forge-status-info","naam":"Gezond","states":{"rest":"forge-status-info-appearance"}},{"component":"forge-app-tile","id":"forge-app-tile-default","naam":"Default App Tile","states":{"disabled":"forge-app-tile-disabled-appearance","focus":"forge-app-tile-focus-appearance","hover":"forge-app-tile-hover-appearance","pressed":"forge-app-tile-pressed-appearance","rest":"forge-app-tile-rest-appearance"}},{"component":"forge-app-tile","id":"forge-app-tile-ember","naam":"Ember App Tile","states":{"disabled":"forge-app-tile-disabled-appearance","focus":"forge-app-tile-ember-focus-appearance","hover":"forge-app-tile-ember-hover-appearance","pressed":"forge-app-tile-ember-pressed-appearance","rest":"forge-app-tile-ember-rest-appearance"}},{"component":"forge-stat-card","id":"forge-stat-card-value","naam":"Value Stat Card","states":{"rest":"forge-stat-card-value-appearance"}},{"component":"forge-stat-card","id":"forge-stat-card-health","naam":"Health Stat Card","states":{"rest":"forge-stat-card-health-appearance"}},{"component":"forge-stat-card","id":"forge-stat-card-progress","naam":"Progress Stat Card","states":{"rest":"forge-stat-card-value-appearance"}},{"component":"forge-stat-card","id":"forge-stat-card-progress-ember","naam":"Ember Progress Stat Card","states":{"rest":"forge-stat-card-ember-appearance"}},{"component":"forge-terminal","id":"forge-terminal-neofetch","naam":"NordForge Neofetch","states":{"rest":"forge-terminal-appearance"}}]};

const COLLECTION_NAMES = Object.freeze({
  primitives: "Color Primitives",
  palette: "Palette",
  material: "Material",
  spacing: "Spacing",
  radius: "Radius",
  border: "Border",
  motion: "Motion",
  typography: "Typography",
});

function assertManifest(manifest) {
  if (!manifest || manifest.schema_version !== 2) {
    throw new Error("Beckeringh Palace Figma adapter requires schema_version 2");
  }
  if (!manifest.product || !/^sha256:[0-9a-f]+$/.test(manifest.product.snapshot || "")) {
    throw new Error("Figma manifest requires a sha256 snapshot identity");
  }
  for (const key of ["theme", "assets", "appearances", "components", "variants", "compositions", "layouts"]) {
    if (!(key in manifest)) throw new Error(`Figma manifest misses '${key}'`);
  }
  return manifest;
}

function px(value) {
  if (value === "0") return 0;
  const match = /^(-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?)px$/.exec(value);
  if (!match) throw new Error(`Expected px value, got '${value}'`);
  return Number(match[1]);
}

function ms(value) {
  const match = /^(0|[1-9][0-9]*(?:\.[0-9]+)?)ms$/.exec(value);
  if (!match) throw new Error(`Expected ms value, got '${value}'`);
  return Number(match[1]);
}

function hexColor(value) {
  const match = /^#([0-9a-fA-F]{6})([0-9a-fA-F]{2})?$/.exec(value);
  if (!match) throw new Error(`Expected hex color, got '${value}'`);
  const rgb = match[1];
  return {
    r: parseInt(rgb.slice(0, 2), 16) / 255,
    g: parseInt(rgb.slice(2, 4), 16) / 255,
    b: parseInt(rgb.slice(4, 6), 16) / 255,
    a: match[2] ? parseInt(match[2], 16) / 255 : 1,
  };
}

function cssCodeSyntax(name) {
  return `var(${name})`;
}

function paletteScopes(role) {
  if (role === "foreground") return ["TEXT_FILL", "SHAPE_FILL"];
  if (role === "background" || role === "surface") return ["FRAME_FILL", "SHAPE_FILL"];
  return ["FRAME_FILL", "SHAPE_FILL", "STROKE_COLOR"];
}

function materialScopes(role) {
  if (role.includes("foreground") || role === "muted" || role === "disabled") {
    return ["TEXT_FILL", "SHAPE_FILL"];
  }
  if (role === "outline") return ["STROKE_COLOR"];
  if (role === "canvas" || role === "surface" || role === "raised" || role === "field" || role === "transparent" || role.endsWith("-surface")) {
    return ["FRAME_FILL", "SHAPE_FILL"];
  }
  return ["FRAME_FILL", "SHAPE_FILL", "STROKE_COLOR"];
}

function splitCssShadows(value) {
  const parts = [];
  let depth = 0;
  let start = 0;
  for (let index = 0; index < value.length; index += 1) {
    const char = value[index];
    if (char === "(") depth += 1;
    if (char === ")") depth -= 1;
    if (char === "," && depth === 0) {
      parts.push(value.slice(start, index).trim());
      start = index + 1;
    }
  }
  parts.push(value.slice(start).trim());
  return parts.filter(Boolean);
}

/** @returns {DropShadowEffect[]} */
function parseCssShadow(value) {
  if (value === "none") return [];
  return splitCssShadows(value).map((part) => {
    const length = "(-?(?:\\d+(?:\\.\\d+)?)(?:px)?)";
    const pattern = new RegExp(
      `^${length}\\s+${length}\\s+${length}(?:\\s+${length})?\\s+` +
      "rgba\\(\\s*(\\d+)\\s*,\\s*(\\d+)\\s*,\\s*(\\d+)\\s*,\\s*(0|1|0?\\.\\d+)\\s*\\)$",
    );
    const match = pattern.exec(part);
    if (!match) throw new Error(`Unsupported shadow '${part}'`);
    const numericLength = (token) => Number(token.replace("px", ""));
    /** @type {DropShadowEffect} */
    const effect = {
      type: "DROP_SHADOW",
      color: {
        r: Number(match[5]) / 255,
        g: Number(match[6]) / 255,
        b: Number(match[7]) / 255,
        a: Number(match[8]),
      },
      offset: {x: numericLength(match[1]), y: numericLength(match[2])},
      radius: numericLength(match[3]),
      spread: match[4] === undefined ? 0 : numericLength(match[4]),
      visible: true,
      blendMode: "NORMAL",
    };
    return effect;
  });
}

function textMetrics(role) {
  const size = px(role.font_size);
  /** @type {LineHeight} */
  const lineHeight = role.line_height === "normal"
    ? {unit: "AUTO"}
    : {unit: "PERCENT", value: Number(role.line_height) * 100};
  /** @type {LetterSpacing} */
  const letterSpacing = role.letter_spacing === "normal"
    ? {unit: "PERCENT", value: 0}
    : {unit: "PERCENT", value: Number(role.letter_spacing.replace("em", "")) * 100};
  return {size, lineHeight, letterSpacing};
}

function desiredState(manifest) {
  assertManifest(manifest);
  const theme = manifest.theme;
  const components = manifest.components.map((component) => {
    const profiles = manifest.variants.filter((variant) => variant.component === component.id);
    const needsBase = manifest.compositions.some((composition) => composition.instances.some(
      (instance) => instance.component === component.id && instance.variant === null,
    ));
    const variants = [];
    if (needsBase) variants.push({profile: "base", state: "rest", appearance: component.appearance});
    for (const profile of profiles) {
      for (const [state, appearance] of Object.entries(profile.states)) {
        variants.push({profile: profile.id, state, appearance});
      }
    }
    return {...component, figmaVariants: variants};
  });
  return {
    schemaVersion: manifest.schema_version,
    snapshot: manifest.product.snapshot,
    collections: {
      primitives: Object.keys(theme.color_primitives).length,
      palette: Object.keys(theme.palet.rollen).length,
      material: Object.keys(theme.materiaal.rollen).length,
      spacing: Object.keys(theme.spacing.rollen).length,
      radius: Object.keys(theme.radius.rollen).length,
      border: Object.keys(theme.border.rollen).length,
      motion: Object.keys(theme.motion.rollen).length,
      typography: Object.keys(theme.typeschaal.rollen).length * 5,
    },
    textStyles: Object.keys(theme.typeschaal.rollen),
    effectStyles: Object.keys(theme.shadow.rollen),
    assets: manifest.assets.map((asset) => asset.id),
    components,
    compositions: manifest.compositions.map((composition) => composition.id),
    layouts: manifest.layouts.map((layout) => layout.id),
  };
}

/** @param {PluginAPI} figmaApi */
async function ensureCollection(figmaApi, name) {
  const collections = await figmaApi.variables.getLocalVariableCollectionsAsync();
  let collection = collections.find((candidate) => candidate.name === name);
  if (!collection) collection = figmaApi.variables.createVariableCollection(name);
  if (collection.modes.length !== 1) {
    throw new Error(`Collection '${name}' must have exactly one mode`);
  }
  if (collection.modes[0].name !== "Value") {
    collection.renameMode(collection.modes[0].modeId, "Value");
  }
  return collection;
}

/** @param {PluginAPI} figmaApi */
async function variableIndex(figmaApi) {
  const variables = await figmaApi.variables.getLocalVariablesAsync();
  const result = new Map();
  for (const variable of variables) {
    result.set(`${variable.variableCollectionId}\u0000${variable.name}`, variable);
  }
  return result;
}

function variableKey(collection, name) {
  return `${collection.id}\u0000${name}`;
}

/** @param {PluginAPI} figmaApi */
function ensureVariable(figmaApi, index, collection, name, type, value, scopes, codeSyntax) {
  const key = variableKey(collection, name);
  let variable = index.get(key);
  if (variable && variable.resolvedType !== type) {
    throw new Error(`Variable '${name}' has type '${variable.resolvedType}', expected '${type}'`);
  }
  if (!variable) {
    variable = figmaApi.variables.createVariable(name, collection, type);
    index.set(key, variable);
  }
  variable.scopes = scopes;
  if (codeSyntax) variable.setVariableCodeSyntax("WEB", codeSyntax);
  variable.setValueForMode(collection.modes[0].modeId, value);
  return variable;
}

/** @param {PluginAPI} figmaApi */
function ensureAlias(figmaApi, index, collection, name, target, scopes, codeSyntax) {
  return ensureVariable(
    figmaApi,
    index,
    collection,
    name,
    "COLOR",
    figmaApi.variables.createVariableAlias(target),
    scopes,
    codeSyntax,
  );
}

/** @param {PluginAPI} figmaApi */
async function syncFoundations(figmaApi, manifest) {
  const theme = manifest.theme;
  const collections = {};
  for (const [key, name] of Object.entries(COLLECTION_NAMES)) {
    collections[key] = await ensureCollection(figmaApi, name);
  }
  const index = await variableIndex(figmaApi);
  const refs = {primitives: {}, palette: {}, material: {}, spacing: {}, radius: {}, border: {}, motion: {}, typography: {}};

  for (const [id, value] of Object.entries(theme.color_primitives)) {
    refs.primitives[id] = ensureVariable(
      figmaApi, index, collections.primitives, id, "COLOR", hexColor(value), [],
      cssCodeSyntax(`--bp-color-${id}`),
    );
  }
  for (const [role, color] of Object.entries(theme.palet.rollen)) {
    const primitive = refs.primitives[color.color_id];
    if (!primitive) throw new Error(`Palette '${role}' references missing color '${color.color_id}'`);
    refs.palette[role] = ensureAlias(
      figmaApi, index, collections.palette, role, primitive, paletteScopes(role),
      cssCodeSyntax(`--bp-theme-${role}`),
    );
  }
  for (const [role, color] of Object.entries(theme.materiaal.rollen)) {
    const primitive = refs.primitives[color.color_id];
    if (!primitive) throw new Error(`Material '${role}' references missing color '${color.color_id}'`);
    refs.material[role] = ensureAlias(
      figmaApi, index, collections.material, role, primitive, materialScopes(role),
      cssCodeSyntax(`--bp-material-${role}`),
    );
  }
  for (const [role, value] of Object.entries(theme.spacing.rollen)) {
    refs.spacing[role] = ensureVariable(
      figmaApi, index, collections.spacing, role, "FLOAT", px(value), ["GAP"],
      cssCodeSyntax(`--bp-spacing-${role}`),
    );
  }
  for (const [role, value] of Object.entries(theme.radius.rollen)) {
    refs.radius[role] = ensureVariable(
      figmaApi, index, collections.radius, role, "FLOAT", px(value), ["CORNER_RADIUS"],
      cssCodeSyntax(`--bp-radius-${role}`),
    );
  }
  for (const [role, value] of Object.entries(theme.border.rollen)) {
    const numeric = role !== "style";
    refs.border[role] = ensureVariable(
      figmaApi, index, collections.border, role, numeric ? "FLOAT" : "STRING",
      numeric ? px(value) : value, numeric ? ["STROKE_FLOAT"] : [],
      cssCodeSyntax(`--bp-border-${role}`),
    );
  }
  for (const [role, value] of Object.entries(theme.motion.rollen)) {
    let normalized = value;
    if (/ms$/.test(value)) normalized = `${ms(value)}ms`;
    refs.motion[role] = ensureVariable(
      figmaApi, index, collections.motion, role, "STRING", normalized, [],
      cssCodeSyntax(`--bp-motion-${role.replace("_", "-")}`),
    );
  }

  for (const [role, definition] of Object.entries(theme.typeschaal.rollen)) {
    const metrics = textMetrics(definition);
    const base = `type/${role}`;
    refs.typography[`${role}/font-family`] = ensureVariable(
      figmaApi, index, collections.typography, `${base}/font-family`, "STRING",
      definition.font_family[0], ["FONT_FAMILY"], cssCodeSyntax(`--bp-type-${role}-font`),
    );
    refs.typography[`${role}/font-size`] = ensureVariable(
      figmaApi, index, collections.typography, `${base}/font-size`, "FLOAT",
      metrics.size, ["FONT_SIZE"], cssCodeSyntax(`--bp-type-${role}`),
    );
    refs.typography[`${role}/font-weight`] = ensureVariable(
      figmaApi, index, collections.typography, `${base}/font-weight`, "FLOAT",
      Number(definition.font_weight), ["FONT_WEIGHT"], cssCodeSyntax(`--bp-type-${role}-weight`),
    );
    const lineHeightValue = definition.line_height === "normal"
      ? 0
      : metrics.size * Number(definition.line_height);
    refs.typography[`${role}/line-height`] = ensureVariable(
      figmaApi, index, collections.typography, `${base}/line-height`, "FLOAT",
      lineHeightValue, ["LINE_HEIGHT"], cssCodeSyntax(`--bp-type-${role}-line-height`),
    );
    const trackingValue = definition.letter_spacing === "normal"
      ? 0
      : metrics.size * Number(definition.letter_spacing.replace("em", ""));
    refs.typography[`${role}/letter-spacing`] = ensureVariable(
      figmaApi, index, collections.typography, `${base}/letter-spacing`, "FLOAT",
      trackingValue, ["LETTER_SPACING"], cssCodeSyntax(`--bp-type-${role}-letter-spacing`),
    );
  }
  return {collections, refs};
}

/** @param {PluginAPI} figmaApi */
async function availableFontName(figmaApi, definition) {
  const allFonts = await figmaApi.listAvailableFontsAsync();
  const family = definition.font_family[0];
  const preferred = {
    "400": ["Regular", "Normal"],
    "500": ["Medium"],
    "600": ["SemiBold", "Semi Bold", "DemiBold", "Demi Bold"],
    "700": ["Bold"],
  }[definition.font_weight] || [];
  for (const style of preferred) {
    if (allFonts.some((entry) => entry.fontName.family === family && entry.fontName.style === style)) {
      return {family, style};
    }
  }
  throw new Error(`Font style for '${family}' weight ${definition.font_weight} is unavailable`);
}

/** @param {PluginAPI} figmaApi */
async function syncTextStyles(figmaApi, manifest) {
  const styles = await figmaApi.getLocalTextStylesAsync();
  const result = {};
  for (const [role, definition] of Object.entries(manifest.theme.typeschaal.rollen)) {
    const name = `Type/${role}`;
    let style = styles.find((candidate) => candidate.name === name);
    if (!style) {
      style = figmaApi.createTextStyle();
      style.name = name;
      styles.push(style);
    }
    const fontName = await availableFontName(figmaApi, definition);
    await figmaApi.loadFontAsync(fontName);
    const metrics = textMetrics(definition);
    style.fontName = fontName;
    style.fontSize = metrics.size;
    style.lineHeight = metrics.lineHeight;
    style.letterSpacing = metrics.letterSpacing;
    style.description = `BAT ${role}; ${definition.font_weight}; ${definition.line_height}; ${definition.letter_spacing}`;
    result[role] = style;
  }
  return result;
}

/** @param {PluginAPI} figmaApi */
async function syncEffectStyles(figmaApi, manifest) {
  const styles = await figmaApi.getLocalEffectStylesAsync();
  const result = {};
  for (const [role, value] of Object.entries(manifest.theme.shadow.rollen)) {
    const name = `Shadow/${role.replace("_", "-")}`;
    let style = styles.find((candidate) => candidate.name === name);
    if (!style) {
      style = figmaApi.createEffectStyle();
      style.name = name;
      styles.push(style);
    }
    style.effects = parseCssShadow(value);
    style.description = `BAT shadow ${role}`;
    result[role.replace("_", "-")] = style;
  }
  return result;
}

/** @param {PluginAPI} figmaApi */
async function ensurePage(figmaApi, name) {
  let page = figmaApi.root.children.find((candidate) => candidate.name === name);
  if (!page) {
    page = figmaApi.createPage();
    page.name = name;
  }
  if (typeof page.loadAsync === "function") await page.loadAsync();
  return page;
}

/** @param {PluginAPI} figmaApi */
async function reclaimDefaultPage(figmaApi, name) {
  const pages = [...figmaApi.root.children];
  let claimed = pages.some((page) => page.name === name);
  for (const page of pages) {
    if (page.name === name) continue;
    if (typeof page.loadAsync === "function") await page.loadAsync();
    if (page.children.length !== 0) continue;
    if (!claimed) {
      page.name = name;
      claimed = true;
    } else {
      page.remove();
    }
  }
}

/** @param {PluginAPI} figmaApi */
async function ensureSection(figmaApi, page, name) {
  let section = /** @type {SectionNode | undefined} */ (page.children.find(
    (candidate) => candidate.type === "SECTION" && candidate.name === name,
  ));
  if (!section) {
    section = figmaApi.createSection();
    section.name = name;
    page.appendChild(section);
  }
  section.fills = [];
  return section;
}

/** @param {PluginAPI} figmaApi */
async function ensurePageLabel(figmaApi, page, name, description, snapshot, textStyle) {
  /** @type {FrameNode | undefined} */
  let frame = /** @type {FrameNode | undefined} */ (page.children.find(
    (node) => node.type === "FRAME" && node.name === "_Documentation",
  ));
  if (!frame) {
    frame = figmaApi.createFrame();
    frame.name = "_Documentation";
    page.appendChild(frame);
  }
  frame.layoutMode = "VERTICAL";
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "FIXED";
  frame.resize(420, 120);
  frame.paddingTop = 24;
  frame.paddingBottom = 24;
  frame.paddingLeft = 24;
  frame.paddingRight = 24;
  frame.itemSpacing = 12;
  frame.fills = [];
  /** @type {TextNode[]} */
  const existing = /** @type {TextNode[]} */ (frame.children.filter(
    (child) => child.type === "TEXT" && child.name === "_GeneratedLabel",
  ));
  while (existing.length > 2) {
    const stale = existing.pop();
    if (stale) stale.remove();
  }
  const labels = [name, `${description}\n${snapshot}`];
  for (let index = 0; index < labels.length; index += 1) {
    let node = existing[index];
    if (!node) {
      node = figmaApi.createText();
      node.name = "_GeneratedLabel";
      frame.appendChild(node);
    }
    const font = textStyle.fontName;
    await figmaApi.loadFontAsync(font);
    node.fontName = font;
    node.fontSize = index === 0 ? Math.max(24, Number(textStyle.fontSize)) : 12;
    node.characters = labels[index];
  }
  return frame;
}

/** @param {PluginAPI} figmaApi */
function boundPaint(figmaApi, variable) {
  return figmaApi.variables.setBoundVariableForPaint(
    {type: "SOLID", color: {r: 0, g: 0, b: 0}}, "color", variable,
  );
}

function appearanceIndex(manifest) {
  return Object.fromEntries(manifest.appearances.map((appearance) => [appearance.id, appearance]));
}

/** @param {PluginAPI} figmaApi */
async function applyAppearance(figmaApi, node, appearance, refs, effectStyles) {
  if (!appearance) return;
  const roles = appearance.rollen;
  if (roles.material && refs.material[roles.material]) node.fills = [boundPaint(figmaApi, refs.material[roles.material])];
  if (roles.outline && refs.material[roles.outline]) node.strokes = [boundPaint(figmaApi, refs.material[roles.outline])];
  if (roles.border && refs.border[roles.border]) node.setBoundVariable("strokeWeight", refs.border[roles.border]);
  if (roles.radius && refs.radius[roles.radius]) {
    for (const property of ["topLeftRadius", "topRightRadius", "bottomLeftRadius", "bottomRightRadius"]) {
      node.setBoundVariable(property, refs.radius[roles.radius]);
    }
  }
  if (roles.spacing && refs.spacing[roles.spacing]) {
    for (const property of ["paddingTop", "paddingRight", "paddingBottom", "paddingLeft", "itemSpacing"]) {
      node.setBoundVariable(property, refs.spacing[roles.spacing]);
    }
  }
  if (roles.shadow && effectStyles[roles.shadow]) {
    await node.setEffectStyleIdAsync(effectStyles[roles.shadow].id);
  }
}

/** @param {PluginAPI} figmaApi */
async function syncComponentVariant(figmaApi, componentNode, definition, appearance, manifest, refs, textStyles, effectStyles) {
  componentNode.layoutMode = "VERTICAL";
  componentNode.primaryAxisSizingMode = "AUTO";
  componentNode.counterAxisSizingMode = "FIXED";
  componentNode.resize(280, Math.max(componentNode.height, 64));
  await applyAppearance(figmaApi, componentNode, appearance, refs, effectStyles);
  const foregroundRole = appearance && appearance.rollen.foreground;
  const foreground = foregroundRole ? refs.material[foregroundRole] : refs.palette.foreground;
  const typeRole = appearance && (appearance.rollen["label-style"] || appearance.rollen["body-style"]);
  const textStyle = textStyles[typeRole || "body"] || textStyles.body;
  for (const anatomy of definition.anatomie) {
    let text = componentNode.children.find((child) => child.type === "TEXT" && child.name === anatomy);
    if (!text) {
      text = figmaApi.createText();
      text.name = anatomy;
      componentNode.appendChild(text);
    }
    await figmaApi.loadFontAsync(textStyle.fontName);
    await text.setTextStyleIdAsync(textStyle.id);
    text.characters = anatomy;
    if (foreground) text.fills = [boundPaint(figmaApi, foreground)];
  }
  componentNode.description = `BAT component ${definition.id}; ${manifest.product.snapshot}`;
}

/** @param {PluginAPI} figmaApi */
async function syncComponents(figmaApi, manifest, refs, textStyles, effectStyles) {
  const appearances = appearanceIndex(manifest);
  const desired = desiredState(manifest);
  const page = await ensurePage(figmaApi, "Components");
  const result = {};
  let sectionY = 40;
  for (const definition of desired.components) {
    const section = await ensureSection(figmaApi, page, `Component / ${definition.naam}`);
    const labelFrame = await ensurePageLabel(figmaApi, section, definition.naam, `BAT component ${definition.id}`, desired.snapshot, textStyles.heading);
    labelFrame.x = 40;
    labelFrame.y = sectionY;
    /** @type {ComponentSetNode | undefined} */
    let componentSet = /** @type {ComponentSetNode | undefined} */ (section.children.find(
      (node) => node.type === "COMPONENT_SET" && node.name === definition.naam,
    ));
    const byName = new Map();
    if (componentSet) {
      for (const child of componentSet.children) byName.set(child.name, child);
    }
    const created = [];
    const refsForComponent = {};
    for (const variant of definition.figmaVariants) {
      const name = `Variant=${variant.profile}, State=${variant.state}`;
      let component = byName.get(name);
      if (!component) {
        component = figmaApi.createComponent();
        component.name = name;
        created.push(component);
      }
      await syncComponentVariant(
        figmaApi, component, definition, appearances[variant.appearance], manifest,
        refs, textStyles, effectStyles,
      );
      refsForComponent[`${variant.profile}:${variant.state}`] = component;
    }
    if (!componentSet) {
      if (!created.length) throw new Error(`Component '${definition.id}' has no Figma variants`);
      componentSet = figmaApi.combineAsVariants(created, section);
      componentSet.name = definition.naam;
      section.appendChild(componentSet);
    } else {
      for (const component of created) componentSet.appendChild(component);
    }
    componentSet.description = `${definition.doel || definition.naam}\nBAT ${definition.id}; ${desired.snapshot}`;
    const ordered = [...componentSet.children].sort((left, right) => left.name.localeCompare(right.name));
    const gap = manifest.theme.spacing ? px(manifest.theme.spacing.rollen.medium) : 16;
    let maxX = 0;
    let maxY = 0;
    for (let index = 0; index < ordered.length; index += 1) {
      const child = ordered[index];
      const column = index % 5;
      const row = Math.floor(index / 5);
      child.x = gap + column * (child.width + gap);
      child.y = gap + row * (child.height + gap);
      maxX = Math.max(maxX, child.x + child.width);
      maxY = Math.max(maxY, child.y + child.height);
    }
    componentSet.resizeWithoutConstraints(maxX + gap, maxY + gap);
    componentSet.x = 500;
    componentSet.y = sectionY;
    section.x = 40;
    section.y = sectionY;
    section.resizeWithoutConstraints(
      Math.max(componentSet.x + componentSet.width, labelFrame.x + labelFrame.width) + gap - section.x,
      Math.max(componentSet.height, labelFrame.height) + gap * 2,
    );
    sectionY += section.height + 120;
    result[definition.id] = {set: componentSet, variants: refsForComponent};
  }
  return result;
}

function svgForAsset(asset) {
  const [x, y, width, height] = asset.viewbox;
  const fill = asset.vulling || "none";
  const stroke = asset.lijn || "none";
  const strokeWidth = asset.lijndikte == null ? "" : ` stroke-width="${asset.lijndikte}"`;
  const linecap = asset.lijneinde ? ` stroke-linecap="${asset.lijneinde}"` : "";
  const linejoin = asset.lijnverbinding ? ` stroke-linejoin="${asset.lijnverbinding}"` : "";
  const paths = asset.paden.map((path) => `<path d="${path}"/>`).join("");
  return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="${x} ${y} ${width} ${height}" fill="${fill}" stroke="${stroke}"${strokeWidth}${linecap}${linejoin}>${paths}</svg>`;
}

/** @param {PluginAPI} figmaApi */
async function syncAssets(figmaApi, manifest, textStyles) {
  const page = await ensurePage(figmaApi, "Assets & Surfaces");
  const section = await ensureSection(figmaApi, page, "Assets");
  const labelFrame = await ensurePageLabel(figmaApi, section, "Assets", `${manifest.assets.length} BAT vector assets`, manifest.product.snapshot, textStyles.heading);
  labelFrame.x = 40;
  labelFrame.y = 40;
  const result = {};
  let x = 500;
  let y = 40;
  let maxX = x;
  let maxY = y;
  for (const asset of manifest.assets) {
    const name = `Asset / ${asset.id}`;
    /** @type {ComponentNode | undefined} */
    let component = /** @type {ComponentNode | undefined} */ (section.children.find(
      (node) => node.type === "COMPONENT" && node.name === name,
    ));
    const marker = `BAT asset ${asset.id}; ${manifest.product.snapshot}`;
    if (!component) {
      component = figmaApi.createComponent();
      component.name = name;
      section.appendChild(component);
    }
    if (component.description !== marker) {
      for (const child of [...component.children]) child.remove();
      const imported = figmaApi.createNodeFromSvg(svgForAsset(asset));
      for (const child of [...imported.children]) component.appendChild(child);
      imported.remove();
      const width = Math.max(1, asset.viewbox[2]);
      const height = Math.max(1, asset.viewbox[3]);
      component.resizeWithoutConstraints(width, height);
      component.description = marker;
    }
    component.x = x;
    component.y = y;
    maxX = Math.max(maxX, x + component.width);
    x += component.width + 48;
    if (x > 1800) {
      x = 500;
      y += 420;
    }
    maxY = Math.max(maxY, y + 420);
    result[asset.id] = component;
  }
  section.x = 40;
  section.y = 40;
  section.resizeWithoutConstraints(
    Math.max(maxX + 48, labelFrame.x + labelFrame.width) - section.x,
    Math.max(maxY, labelFrame.y + labelFrame.height + 40) - section.y,
  );
  return result;
}

function matchingComposition(layout, compositions) {
  const layoutInstances = [...layout.regions.map((region) => region.instance)].sort();
  const matches = compositions.filter((composition) => {
    const compositionInstances = [...composition.instances.map((instance) => instance.id)].sort();
    return JSON.stringify(compositionInstances) === JSON.stringify(layoutInstances);
  });
  if (matches.length !== 1) {
    throw new Error(`Layout '${layout.id}' must map to exactly one composition`);
  }
  return matches[0];
}

function componentForInstance(componentRefs, instance) {
  const ref = componentRefs[instance.component];
  if (!ref) throw new Error(`Missing Figma component '${instance.component}'`);
  const key = `${instance.variant || "base"}:rest`;
  const component = ref.variants[key];
  if (!component) throw new Error(`Missing Figma variant '${instance.component}' '${key}'`);
  return component;
}

/** @param {PluginAPI} figmaApi */
async function syncSurfaces(figmaApi, manifest, refs, componentRefs, textStyles) {
  const page = await ensurePage(figmaApi, "Assets & Surfaces");
  /** @type {SectionNode | undefined} */
  const assetsSection = /** @type {SectionNode | undefined} */ (page.children.find(
    (node) => node.type === "SECTION" && node.name === "Assets",
  ));
  const baseY = assetsSection ? assetsSection.y + assetsSection.height + 120 : 40;
  const section = await ensureSection(figmaApi, page, "Surfaces");
  const labelFrame = await ensurePageLabel(figmaApi, section, "Surfaces", `${manifest.layouts.length} native BAT layouts`, manifest.product.snapshot, textStyles.heading);
  labelFrame.x = 40;
  labelFrame.y = baseY;
  const gap = px(manifest.theme.spacing.rollen.medium);
  let surfaceY = baseY + 180;
  const result = {};
  for (const layout of manifest.layouts) {
    const composition = matchingComposition(layout, manifest.compositions);
    const name = `Surface / ${layout.id}`;
    /** @type {FrameNode | undefined} */
    let frame = /** @type {FrameNode | undefined} */ (section.children.find(
      (node) => node.type === "FRAME" && node.name === name,
    ));
    if (!frame) {
      frame = figmaApi.createFrame();
      frame.name = name;
      section.appendChild(frame);
    }
    /** @type {Record<string, InstanceNode>} */
    const instances = {};
    for (const instance of composition.instances) {
      const target = componentForInstance(componentRefs, instance);
      const named = frame.children.find((child) => child.name === instance.id);
      if (named && named.type !== "INSTANCE") {
        throw new Error(`Surface '${layout.id}' contains incompatible node '${instance.id}'`);
      }
      const node = named || target.createInstance();
      if (node.type !== "INSTANCE") throw new Error(`Surface '${layout.id}' requires Figma instances`);
      if (!named) {
        node.name = instance.id;
        frame.appendChild(node);
      } else {
        node.swapComponent(target);
      }
      node.resizeWithoutConstraints(target.width, target.height);
      instances[instance.id] = node;
    }
    if (layout.type === "grid") {
      const cellWidth = Math.max(...Object.values(instances).map((node) => node.width));
      const cellHeight = Math.max(...Object.values(instances).map((node) => node.height));
      for (const region of layout.regions) {
        const node = instances[region.instance];
        node.x = gap + (region.column - 1) * (cellWidth + gap);
        node.y = gap + (region.row - 1) * (cellHeight + gap);
        node.resizeWithoutConstraints(
          cellWidth * region.column_span + gap * (region.column_span - 1),
          cellHeight * region.row_span + gap * (region.row_span - 1),
        );
      }
      frame.resizeWithoutConstraints(
        gap + layout.columns * (cellWidth + gap),
        gap + layout.rows * (cellHeight + gap),
      );
    } else {
      frame.layoutMode = layout.direction === "horizontal" ? "HORIZONTAL" : "VERTICAL";
      frame.primaryAxisSizingMode = "AUTO";
      frame.counterAxisSizingMode = "AUTO";
      frame.itemSpacing = gap;
      frame.paddingTop = gap;
      frame.paddingRight = gap;
      frame.paddingBottom = gap;
      frame.paddingLeft = gap;
    }
    if (refs.material.canvas) frame.fills = [boundPaint(figmaApi, refs.material.canvas)];
    frame.x = 40;
    frame.y = surfaceY;
    surfaceY += frame.height + 120;
    result[layout.id] = frame;
  }
  section.x = 40;
  section.y = baseY;
  section.resizeWithoutConstraints(
    Math.max(labelFrame.x + labelFrame.width, 900) - section.x,
    surfaceY - baseY,
  );
  return result;
}

/** @param {PluginAPI} figmaApi */
async function syncFileStructure(figmaApi, manifest, refs, textStyles) {
  const page = await ensurePage(figmaApi, "Foundations");
  let sectionY = 40;

  const cover = await ensureSection(figmaApi, page, "Cover");
  const coverFrame = await ensurePageLabel(
    figmaApi, cover, manifest.figma_master.naam, manifest.figma_master.doel,
    manifest.product.snapshot, textStyles.title,
  );
  coverFrame.x = 40;
  coverFrame.y = sectionY;
  if (refs.material.canvas) coverFrame.fills = [boundPaint(figmaApi, refs.material.canvas)];
  cover.x = 40;
  cover.y = sectionY;
  cover.resizeWithoutConstraints(Math.max(coverFrame.width, 480) + 80, coverFrame.height + 80);
  sectionY += cover.height + 80;

  const gettingStarted = await ensureSection(figmaApi, page, "Getting Started");
  const gettingStartedFrame = await ensurePageLabel(
    figmaApi, gettingStarted, "Getting Started",
    `Generated from ${manifest.product.id} ${manifest.product.snapshot}`,
    manifest.product.snapshot, textStyles.heading,
  );
  gettingStartedFrame.x = 40;
  gettingStartedFrame.y = sectionY;
  gettingStarted.x = 40;
  gettingStarted.y = sectionY;
  gettingStarted.resizeWithoutConstraints(Math.max(gettingStartedFrame.width, 480) + 80, gettingStartedFrame.height + 80);
  sectionY += gettingStarted.height + 80;

  const foundations = await ensureSection(figmaApi, page, "Foundations");
  const foundationFrame = await ensurePageLabel(
    figmaApi, foundations, "Foundations",
    "Color primitives, semantic colors, spacing, radius, borders, motion, typography and shadows are native Figma foundations.",
    manifest.product.snapshot, textStyles.heading,
  );
  foundationFrame.x = 40;
  foundationFrame.y = sectionY;
  if (refs.material.surface) foundationFrame.fills = [boundPaint(figmaApi, refs.material.surface)];
  foundations.x = 40;
  foundations.y = sectionY;
  foundations.resizeWithoutConstraints(Math.max(foundationFrame.width, 480) + 80, foundationFrame.height + 80);
}

function assertExactNames(kind, actual, expected) {
  const normalizedActual = [...actual].sort();
  const normalizedExpected = [...expected].sort();
  if (JSON.stringify(normalizedActual) !== JSON.stringify(normalizedExpected)) {
    throw new Error(
      `${kind} mismatch: expected ${JSON.stringify(normalizedExpected)}, got ${JSON.stringify(normalizedActual)}`,
    );
  }
}

/** @param {PluginAPI} figmaApi */
async function exactPage(figmaApi, name) {
  const pages = figmaApi.root.children.filter((page) => page.name === name);
  if (pages.length !== 1) {
    throw new Error(`Expected exactly one Figma page '${name}', got ${pages.length}`);
  }
  const page = pages[0];
  if (typeof page.loadAsync === "function") await page.loadAsync();
  return page;
}

/** @param {PluginAPI} figmaApi */
async function exactSection(figmaApi, pageName, sectionName) {
  const page = await exactPage(figmaApi, pageName);
  const sections = page.children.filter(
    (node) => node.type === "SECTION" && node.name === sectionName,
  );
  if (sections.length !== 1) {
    throw new Error(`Expected exactly one Figma section '${sectionName}' on page '${pageName}', got ${sections.length}`);
  }
  return sections[0];
}

function normalizedVariableValue(value) {
  if (value && typeof value === "object" && value.type === "VARIABLE_ALIAS") {
    return {type: value.type, id: value.id};
  }
  if (value && typeof value === "object") {
    return Object.fromEntries(Object.entries(value).sort(([left], [right]) => left.localeCompare(right)));
  }
  return value;
}

/** @param {PluginAPI} figmaApi */
async function verifyLiveState(figmaApi, manifest) {
  const desired = desiredState(manifest);
  const collections = await figmaApi.variables.getLocalVariableCollectionsAsync();
  const variables = await figmaApi.variables.getLocalVariablesAsync();
  const managedCollections = [];
  for (const [key, name] of Object.entries(COLLECTION_NAMES)) {
    const matches = collections.filter((collection) => collection.name === name);
    if (matches.length !== 1) {
      throw new Error(`Expected exactly one variable collection '${name}', got ${matches.length}`);
    }
    const collection = matches[0];
    if (collection.modes.length !== 1 || collection.modes[0].name !== "Value") {
      throw new Error(`Variable collection '${name}' must have exactly one 'Value' mode`);
    }
    const owned = variables
      .filter((variable) => variable.variableCollectionId === collection.id)
      .sort((left, right) => left.name.localeCompare(right.name));
    if (owned.length !== desired.collections[key]) {
      throw new Error(
        `Variable collection '${name}' expected ${desired.collections[key]} variables, got ${owned.length}`,
      );
    }
    const modeId = collection.modes[0].modeId;
    managedCollections.push({
      name,
      variables: owned.map((variable) => ({
        id: variable.id,
        name: variable.name,
        type: variable.resolvedType,
        scopes: [...variable.scopes].sort(),
        web: variable.codeSyntax.WEB || null,
        value: normalizedVariableValue(variable.valuesByMode[modeId]),
      })),
    });
  }

  const localTextStyles = await figmaApi.getLocalTextStylesAsync();
  const textStyleNames = desired.textStyles.map((role) => `Type/${role}`);
  const textStyles = localTextStyles.filter((style) => textStyleNames.includes(style.name));
  assertExactNames("Text styles", textStyles.map((style) => style.name), textStyleNames);
  const textStyleState = textStyles
    .map((style) => ({
      id: style.id,
      name: style.name,
      fontName: style.fontName,
      fontSize: style.fontSize,
      lineHeight: style.lineHeight,
      letterSpacing: style.letterSpacing,
      description: style.description,
    }))
    .sort((left, right) => left.name.localeCompare(right.name));

  const localEffectStyles = await figmaApi.getLocalEffectStylesAsync();
  const effectStyleNames = desired.effectStyles.map((role) => `Shadow/${role.replace("_", "-")}`);
  const effectStyles = localEffectStyles.filter((style) => effectStyleNames.includes(style.name));
  assertExactNames("Effect styles", effectStyles.map((style) => style.name), effectStyleNames);
  const effectStyleState = effectStyles
    .map((style) => ({id: style.id, name: style.name, effects: style.effects, description: style.description}))
    .sort((left, right) => left.name.localeCompare(right.name));

  const componentState = [];
  for (const definition of desired.components) {
    const section = await exactSection(figmaApi, "Components", `Component / ${definition.naam}`);
    const sets = /** @type {ComponentSetNode[]} */ (section.children.filter(
      (node) => node.type === "COMPONENT_SET" && node.name === definition.naam,
    ));
    if (sets.length !== 1) {
      throw new Error(`Component '${definition.id}' expected exactly one component set, got ${sets.length}`);
    }
    const set = sets[0];
    const expectedVariants = definition.figmaVariants.map(
      (variant) => `Variant=${variant.profile}, State=${variant.state}`,
    );
    assertExactNames(
      `Component '${definition.id}' variants`,
      set.children.map((child) => child.name),
      expectedVariants,
    );
    if (!set.description.includes(desired.snapshot)) {
      throw new Error(`Component '${definition.id}' does not carry snapshot '${desired.snapshot}'`);
    }
    for (const child of set.children) {
      if (child.type !== "COMPONENT" || !child.description.includes(desired.snapshot)) {
        throw new Error(`Component '${definition.id}' contains an unverified variant '${child.name}'`);
      }
    }
    componentState.push({
      id: set.id,
      name: definition.id,
      variants: [...set.children].map((child) => ({id: child.id, name: child.name})).sort(
        (left, right) => left.name.localeCompare(right.name),
      ),
    });
  }

  const assetsSection = await exactSection(figmaApi, "Assets & Surfaces", "Assets");
  const assetNames = desired.assets.map((id) => `Asset / ${id}`);
  const assetNodes = /** @type {ComponentNode[]} */ (assetsSection.children.filter(
    (node) => node.type === "COMPONENT" && assetNames.includes(node.name),
  ));
  assertExactNames("Assets", assetNodes.map((node) => node.name), assetNames);
  for (const node of assetNodes) {
    if (!node.description.includes(desired.snapshot)) {
      throw new Error(`Asset '${node.name}' does not carry snapshot '${desired.snapshot}'`);
    }
  }
  const assetState = assetNodes
    .map((node) => ({id: node.id, name: node.name, width: node.width, height: node.height, children: node.children.length}))
    .sort((left, right) => left.name.localeCompare(right.name));

  const surfacesSection = await exactSection(figmaApi, "Assets & Surfaces", "Surfaces");
  const surfaceNames = desired.layouts.map((id) => `Surface / ${id}`);
  const surfaceNodes = /** @type {FrameNode[]} */ (surfacesSection.children.filter(
    (node) => node.type === "FRAME" && surfaceNames.includes(node.name),
  ));
  assertExactNames("Surfaces", surfaceNodes.map((node) => node.name), surfaceNames);
  const surfaceState = (await Promise.all(surfaceNodes.map(async (node) => {
    const instanceNodes = /** @type {InstanceNode[]} */ (node.children
      .filter((child) => child.type === "INSTANCE"));
    const instances = await Promise.all(instanceNodes.map(async (child) => {
      const mainComponent = await child.getMainComponentAsync();
      return {id: child.id, name: child.name, mainComponent: mainComponent && mainComponent.id};
    }));
    return {
      id: node.id,
      name: node.name,
      width: node.width,
      height: node.height,
      instances: instances.sort((left, right) => left.name.localeCompare(right.name)),
    };
  }))).sort((left, right) => left.name.localeCompare(right.name));

  const documentedSections = [
    {page: "Foundations", section: "Cover"},
    {page: "Foundations", section: "Getting Started"},
    {page: "Foundations", section: "Foundations"},
    {page: "Assets & Surfaces", section: "Assets"},
    {page: "Assets & Surfaces", section: "Surfaces"},
  ];
  for (const {page: pageName, section: sectionName} of documentedSections) {
    const section = await exactSection(figmaApi, pageName, sectionName);
    const documentation = /** @type {FrameNode | undefined} */ (section.children.find(
      (node) => node.type === "FRAME" && node.name === "_Documentation",
    ));
    const labels = documentation && /** @type {TextNode[]} */ (documentation.children.filter(
      (child) => child.type === "TEXT" && child.name === "_GeneratedLabel",
    ));
    if (!labels || !labels.some((label) => label.characters.includes(desired.snapshot))) {
      throw new Error(`Section '${sectionName}' on page '${pageName}' does not document snapshot '${desired.snapshot}'`);
    }
  }

  return {
    snapshot: desired.snapshot,
    collections: managedCollections,
    textStyles: textStyleState,
    effectStyles: effectStyleState,
    components: componentState,
    assets: assetState,
    surfaces: surfaceState,
  };
}

/** @param {PluginAPI} figmaApi */
async function runSync(figmaApi, manifest) {
  assertManifest(manifest);
  await reclaimDefaultPage(figmaApi, "Foundations");
  const foundations = await syncFoundations(figmaApi, manifest);
  const textStyles = await syncTextStyles(figmaApi, manifest);
  const effectStyles = await syncEffectStyles(figmaApi, manifest);
  await syncFileStructure(figmaApi, manifest, foundations.refs, textStyles);
  const componentRefs = await syncComponents(
    figmaApi, manifest, foundations.refs, textStyles, effectStyles,
  );
  await syncAssets(figmaApi, manifest, textStyles);
  await syncSurfaces(figmaApi, manifest, foundations.refs, componentRefs, textStyles);
  const plan = desiredState(manifest);
  return {
    snapshot: plan.snapshot,
    collections: Object.keys(plan.collections).length,
    variables: Object.values(plan.collections).reduce((sum, value) => sum + value, 0),
    textStyles: plan.textStyles.length,
    effectStyles: plan.effectStyles.length,
    assets: plan.assets.length,
    componentFamilies: plan.components.length,
    figmaVariants: plan.components.reduce((sum, component) => sum + component.figmaVariants.length, 0),
    surfaces: plan.layouts.length,
  };
}

function firstDifferingPath(first, second, path = "$") {
  if (first === second) return null;
  const firstIsObject = first !== null && typeof first === "object";
  const secondIsObject = second !== null && typeof second === "object";
  if (!firstIsObject || !secondIsObject) {
    return {path, first, second};
  }
  const firstKeys = Array.isArray(first)
    ? first.map((_, index) => String(index))
    : Object.keys(first);
  const secondKeys = Array.isArray(second)
    ? second.map((_, index) => String(index))
    : Object.keys(second);
  const keys = [...new Set([...firstKeys, ...secondKeys])];
  for (const key of keys) {
    const found = firstDifferingPath(first[key], second[key], `${path}.${key}`);
    if (found) return found;
  }
  return null;
}

/** @param {PluginAPI} figmaApi */
async function runVerifiedSync(figmaApi, manifest) {
  const firstSummary = await runSync(figmaApi, manifest);
  const firstState = await verifyLiveState(figmaApi, manifest);
  const secondSummary = await runSync(figmaApi, manifest);
  const secondState = await verifyLiveState(figmaApi, manifest);
  if (JSON.stringify(firstState) !== JSON.stringify(secondState)) {
    const diff = firstDifferingPath(firstState, secondState);
    console.error("Beckeringh Palace idempotency diff:", diff);
    throw new Error(
      `Second Figma sync changed managed state; adapter is not idempotent at ` +
      `${diff ? diff.path : "(unknown path)"} (zie console voor de volledige waarden)`,
    );
  }
  return {...secondSummary, verified: true, idempotent: true, firstSummary};
}

if (typeof figma !== "undefined") {
  runVerifiedSync(figma, BP_MANIFEST)
    .then((summary) => figma.closePlugin(
      `Beckeringh Palace verified ${summary.variables} variables, ${summary.componentFamilies} component families, ${summary.assets} assets and ${summary.surfaces} surfaces; second sync is idempotent.`,
    ))
    .catch((error) => {
      console.error("Beckeringh Palace sync failed:", error);
      figma.closePlugin(`Beckeringh Palace sync failed: ${error.message} (zie console voor details)`);
    });
}

if (typeof module !== "undefined" && module.exports) {
  module.exports = {
    manifest: BP_MANIFEST,
    assertManifest,
    desiredState,
    parseCssShadow,
    runSync,
    verifyLiveState,
    runVerifiedSync,
  };
}
